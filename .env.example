# Copy to .env and fill in. .env is gitignored and never committed.
# On CI these are provided as GitHub Secrets (see .github/workflows/playwright.yml).

# A registered account on https://automationexercise.com used by the
# login, checkout and payment tests.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
TEST_USER_NAME=
